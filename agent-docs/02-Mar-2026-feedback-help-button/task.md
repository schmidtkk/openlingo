Feedback / help button in OpenLingo that will work both without being logged in and also inside the app. If not logged in this button will only show once the cloudflare turnstile is successful. It will ask the user for their email and then a free text area to give feedback or ask for help. Inside the app it will just be the free text area and you populate name & email from auth information, also add information about if the user was logged in via google auth or email/passowrd. Then send the feedback/help request to slack using SLACK_WEBHOOK env var
